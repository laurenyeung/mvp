# Frontend — production (Vercel)
# VITE_ vars are safe to commit — they are embedded in the JS bundle and publicly visible.
# Set this to your deployed backend URL before deploying.
VITE_API_URL=https://api.lockedinforlife.com/api/v1
